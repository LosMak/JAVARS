/*Задача №4
Дана строка: 
String s = “Перестановочный алгоритм быстрого действия”;
необходимо вывести все буквы “о” из этой строки.
Для указанной строки ответ будет “ооооо” (или в столбик)
*/	
public class HW4_4 {
        @SuppressWarnings("empty-statement")
	public static void main(String[] args) {
		String s = "Перевыборы выбранного президента";

                for (int i = 0; i < s.length(); i++) {
                    char c= s.charAt(i);
                    if (c == 'о' ) {
                        System.out.print(c);
                    }
                }
/*Задача №5
Дана строка:
String s = “Перевыборы выбранного президента”;
необходимо подсчитать количество букв “е” в строке и вывести это количество на экран.
Для указанной строки ответ будет 4.*/
                int count = 0;
                for (int i = 0; i < s.length(); i++) {
                    char c= s.charAt(i);
                    if (c == 'е' ) {
                        count++;
                       System.out.print(c);
                    }
                }
                System.out.println("Количество букв 'е' в строке: " + count);
/*Задача №6
Дан массив:
int[] array = {9, 2, 6, 4, 5, 12, 7, 8, 6};
необходимо вывести сумму всех значений массива.*/
                int[] array = {9, 2, 6, 4, 5, 12, 7, 8, 6};
                int sum = 0;
                for (int i = 0; i < array.length; i++) {
                    sum += array[i];
                }
                System.out.println("Сумма всех значений массива: " + sum);
/*Задача №7
(немного подумать придется)
Возвращаясь к маньяку со спичками с лекции.
Имеется 20 коробок спичек. Найти все коробки, количество спичек в которых отличается от среднего количества больше, чем на 3. Например, если есть следующие данные:60, 59, 58, 59, 64, 60, 
то среднее количество будет чуть меньше 60. Соответственно, коробка с 64 спичками будет сильно отличаться от среднего.*/    
                int[] boxes = {60, 59, 58, 59, 64, 60, 57, 62, 61, 55, 63, 59, 58, 60, 61, 57, 65, 59, 60, 58};
                int total = 0;
                for (int i = 0; i < boxes.length; i++) {
                    total += boxes[i];
                }
                double average = (double) total / boxes.length;
                System.out.println("Среднее количество спичек в коробках: " + average);
                System.out.println("Коробки с количеством спичек, отличающимся от среднего более чем на 3:");
                for (int i = 0; i < boxes.length; i++) {
                    if (Math.abs(boxes[i] - average) > 3) {
                        System.out.println("Коробка " + (i + 1) + " с " + boxes[i] + " спичками");
                    }
                }


	}
}
